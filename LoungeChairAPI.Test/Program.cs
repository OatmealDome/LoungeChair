using System.Threading.Tasks;

namespace LoungeChairAPI.Test
{
    class Program
    {
        static void Main(string[] args)
        {
            RealMain().Wait();
        }

        static async Task RealMain()
        {
            //NintendoAccount account = NintendoAccount.Login(access, id);
            //OnlineLounge lounge = await OnlineLounge.Login(account);
            //List<WebService> services = await lounge.GetWebServices();
            ;
        }
    }
}
